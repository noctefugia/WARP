This batch file, when executed, will download the TinyCC toolchain from the official site, extract it to a specified folder, copy additional files from a designated "addon" folder, and then run a build script to build all WARP binaries automatically.

1.  Run the batch file (may require admin rights).
2.  The batch file will then download the TinyCC toolchain, extract it to the specified folder, copy additional files from the "addon" folder, and then run the build script.