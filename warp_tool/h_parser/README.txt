This batch file, when executed, will parse C-header files extracted from Ghidra disassembler and form a list of structures.

1. Drag and drop the C-header file onto the batch file.
2. The batch file will then extract the structure names from the file and append them to an output file named "output.txt".

The output file, "output.txt", will contain the structure names extracted from the input file.